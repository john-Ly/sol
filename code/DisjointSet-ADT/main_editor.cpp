#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

// @TODO 
 
int maxParty(const BipartiteDisjoint& buf, int numElements){
	int res = 0;
	for(int node=0; node<numElements; ++node){
		if(buf.find(node) < 0) 
	}
	
}

int main(int argc, char** argv) {
	int numElements = 128;
	
 	BipartitrDisjoint ds(numElements);
 	maxParty(ds, numElements)
	return 0;
}


